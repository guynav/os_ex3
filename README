H
Q1
The optimal number of threads is the number of the computer's cpu.
Since the bottleneck of the program is the cpu, it means that no matter how many therads we are going to have,
The hardwere of the computer will be the main limitation.

Q2
Since every thread is being duplicated by the fork function, the line is going to be printed 2n times.
the first n lines by the child threads and the other half by the child threads after the

Q3
The children threads have n! options of printing and the parents thread have n! option, therefor there are n!*n! = n!^2 options

